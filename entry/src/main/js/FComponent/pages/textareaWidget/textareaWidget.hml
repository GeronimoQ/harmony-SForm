<div class="widget-container" >
    <label class="label" target="{{ widgetConfig.id }}">
        {{ widgetConfig.options.label }}
    </label>

    <div class="fill-widget" >
        <textarea @change="valueChange" placeholder="{{widgetConfig.options.placeholder}}" value="{{widgetInfo.value}}" showcounter="true" extend="true"></textarea>
    </div >
</div>
