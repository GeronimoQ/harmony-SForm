<div class="widget-container">

    <div class="label">
        <label target="{{ widgetConfig.id }}">
            {{ widgetConfig.options.label }}
        </label>

        <rating class="label-fill-widget" @change="rateChange" numstars="{{widgetConfig.options.max}}" rating="{{widgetConfig.options.defaultValue}}" >

        </rating>
    </div>
</div>
